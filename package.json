{
  "name": "borrowedlens-space",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "express": "^4.17.1",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "react-scripts": "3.4.1",
    "styled-components": "^5.1.1"
  },
  "scripts": {
    "start": "SET NODE_ENV=development&&node server.js",
    "build-dev": "SET NODE_ENV=development&&webpack",
    "quick-dev": "SET NODE_ENV=development&&webpack&&SET NODE_ENV=development&&node server.js",
    "build": "SET NODE_ENV=production&&webpack --config webpack.config.prod.js",
    "quick": "SET NODE_ENV=production&&webpack --config webpack.config.prod.js&&SET NODE_ENV=production&&node server.js",
    "react-start": "react-scripts start"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "babel-core": "^6.26.3",
    "babel-loader": "^8.1.0",
    "babel-preset-react-app": "^9.1.2",
    "webpack": "^4.43.0",
    "webpack-cli": "^3.3.12"
  }
}
